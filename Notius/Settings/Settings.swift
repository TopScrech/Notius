import SwiftUI

struct Settings: View {
    var body: some View {
        List {
            
        }
        .navigationTitle("Settings")
        .toolbarTitleDisplayMode(.inline)
    }
}

#Preview {
    Settings()
}
